<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.weatherappnew">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".cityview_row"></activity>
        <activity android:name=".StartActivity" />
        <activity android:name=".ShowSevenDayRow" />
        <activity android:name=".SQLiteOpenHelper" />
        <activity android:name=".PrefManager" />
        <activity android:name=".MyWeatherViewAdapter" />
        <activity android:name=".MyWeatherDetailViewAdapter" />
        <activity android:name=".DetailWeatherPro" />
        <activity android:name=".IntroPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>